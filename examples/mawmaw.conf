# ── MAWMAW topology configuration ────────────────────────────────────────────
#
# Each section ([ingestor], [script], [publisher]) is one component.
# Sections of the same type can repeat. Order doesn't matter.
#
# Window format:  stream_id, count|time, N
#   count — last N events from that stream
#   time  — events from last N nanoseconds
#
# Publisher types: stdout | file | null
#   file requires: path = ./output.log
# ─────────────────────────────────────────────────────────────────────────────

[ingestor]
id     = dummy
plugin = ./plugin_dummy.so

[publisher]
id   = telemetry
type = telemetry


# [publisher]
# id   = stdout
# type = stdout

# [publisher]
# id   = event_log
# type = file
# path = ./mawmaw_events.log

# [publisher]
# id   = benchsink
# type = null


# [script]
# id      = wasm_passthrough
# runtime = wasm
# path    = ./scripts/passthrough.wasm
# trigger = dummy_ticks
# window  = dummy_ticks, count, 16

# [script]
# id      = wasm_passthrough2
# runtime = wasm
# path    = ./scripts/passthrough.wasm
# trigger = dummy_ticks
# window  = dummy_ticks, count, 16

# [script]
# id      = wasm_passthrough3
# runtime = wasm
# path    = ./scripts/passthrough.wasm
# trigger = dummy_ticks
# window  = dummy_ticks, count, 16

# [script]
# id      = wasm_passthrough4
# runtime = wasm
# path    = ./scripts/passthrough.wasm
# trigger = dummy_ticks
# window  = dummy_ticks, count, 16

[script]
id      = wasm_passthrough5
runtime = wasm
path    = ./scripts/passthrough.wasm
trigger = dummy_ticks
window  = dummy_ticks, count, 16

# [script]
# id      = py_passthrough
# runtime = python
# path    = ./scripts/passthrough.py
# trigger = dummy_ticks
# window  = dummy_ticks, count, 16
